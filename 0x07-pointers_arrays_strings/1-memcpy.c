#include "main.h"
/**
 *_memcpy - a function that copies memory area
 *@dest: memory stored .
 *@src: memory copied .
 *@n: number of bytes
 *
 *Return: dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int x = n;
	int s = 0;

	for (; r < x; s++)
	{
		dest[s] = src[s];
		n--;
	}
	return (dest);
}
